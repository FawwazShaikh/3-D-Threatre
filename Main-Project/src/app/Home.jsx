import App from '../App';

/**
 * Home page — wraps the existing Hero (App.jsx) as the landing section.
 * Fan Deck and marketing sections will be added below.
 */
export default function Home() {
  return <App />;
}
